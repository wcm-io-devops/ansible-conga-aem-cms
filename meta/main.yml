dependencies:
  - conga-facts
  - {
      role: conga-files,
      base_path: "{{ conga_config.quickstart.rootPath }}/crx-quickstart",
      owner: "{{ aem_user }}",
      group: "{{ aem_group }}",
      mode: "0755",
      handlers: [ restart aem ]
    }
  - { role: conga-aem-packages,
      aem_port: "{{ conga_config.quickstart.port }}" }
